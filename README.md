# Open Verification Kernel

Open Verification Kernel is an open-source, solver-agnostic verification layer for AI-agent engineering workflows.

This repository is being initialized with the project documentation, schemas, templates, examples, and starter implementation so engineers can take over the build.
