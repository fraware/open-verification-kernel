# Sprint 4 CLI Note

The infrastructure exposure path now has a first-class OVK CLI command.

Implemented:

- `ovk infra-exposure`.
- CLI tests for public sensitive resource exposure.
- CLI tests for private sensitive resources.
- Evidence, Markdown, and attestation output support.

The standalone script remains available as `scripts/run_infra_exposure.py`.
