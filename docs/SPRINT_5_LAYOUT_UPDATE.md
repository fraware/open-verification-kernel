# Sprint 5 Layout Update

Release layout support has started.

Implemented:

- Added `ovk.core.release_layout`.
- Added `docs/RELEASE_ARTIFACT_LAYOUT.md`.

Current release artifacts:

- evidence bundle;
- Markdown report;
- unsigned attestation statement;
- artifact manifest;
- attestation envelope.

Current limitation:

The release-layout tests were blocked by connector controls. The helper remains small and deterministic.
